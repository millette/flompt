fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
python-dotenv==1.0.1
httpx==0.27.0

# AI providers (décommenter selon ton choix)
# anthropic==0.34.0
# openai==1.45.0

# Prompt Guard — filtre de sécurité (meta-llama/Llama-Guard-4-12B)
# Nécessite HUGGINGFACE_TOKEN (modèle gated Meta).
# Si non installé ou si le token est absent, le guard se désactive (fail-open).
#
# Install :
#   pip install "git+https://github.com/huggingface/transformers@v4.51.3-LlamaGuard-preview" hf_xet torch
#
# Remplace la ligne ci-dessous par le lien git si tu n'as pas encore transformers ≥ 4.51.3 :
transformers>=4.51.3
hf_xet
torch>=2.2.0
