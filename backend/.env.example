# ─── AI Provider ─────────────────────────────────────────────────────────────
# Décommenter et remplir selon ton provider

# Groq (recommandé — rapide et gratuit)
# GROQ_API_KEY=gsk_...
# AI_PROVIDER=groq
# AI_MODEL=openai/gpt-oss-120b

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# AI_PROVIDER=anthropic
# AI_MODEL=claude-sonnet-4-20250514

# OpenAI
# OPENAI_API_KEY=sk-...
# AI_PROVIDER=openai
# AI_MODEL=gpt-4o-mini

# ─── Prompt Guard (filtre de sécurité) ───────────────────────────────────────
# llama-guard-3-8b via Groq — analyse les prompts (S1–S13 hazard taxonomy).
# Utilise le même GROQ_API_KEY — aucune clé supplémentaire requise.
# Désactiver avec PROMPT_GUARD_ENABLED=false.
# PROMPT_GUARD_ENABLED=true
